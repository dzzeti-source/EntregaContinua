{
  "name": "meu-site-frontend",
  "version": "1.0.0",
  "description": "Site estático com CI/CD",
  "scripts": {
    "lint": "echo 'Linting: nenhum erro encontrado'",
    "test": "echo 'Testes: todos aprovados!'",
    "build": "mkdir -p build && cp index.html build/ && echo 'Build concluído com sucesso!'"
  },
  "devDependencies": {
    "htmlhint": "^1.1.4"
  }
}
